<div class="ping-show">
  <div class="headline">
    <span class="type"><%= @ping.is_ping ? 'PING' : "HTTP #{@ping.http_method}" %></span>
    <span class="url"><%= @ping.url %></span>
    <a class="button right" href="/ping/<%= @ping.id %>/edit">Edit</a>
  </div>
  <div id="chart"></div>
</div>
<script type="text/javascript">
columns = [
  ['time', "<%= @ping.timeouts_log.keys.join('", "') %>"],
  ['response time', <%= @ping.timeouts_log.values.join(',') %>]
];

regions = <%= @ping.incidents_regions.to_json %>;
</script>
